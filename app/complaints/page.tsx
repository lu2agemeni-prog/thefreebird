import { redirect } from 'next/navigation';

// ============================================================================
// app/complaints/page.tsx
// كانت هذه الصفحة "وهمية": نموذج لا يُرسل شيئًا للقاعدة (alert فقط!) وكان
// مرتبطة من الصفحة الرئيسية وبطاقات QR. الآن تحوّل الزائر إلى نموذج
// الشكاوى الحقيقي والمحمي في /public/complaints.
// ============================================================================

export default function ComplaintsPage() {
  redirect('/public/complaints');
}
